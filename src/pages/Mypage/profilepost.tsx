/** @format */

import { Input } from "@/components/ui/form";
import { Camera } from "lucide-react";

const ProfilePost = () => {
  return (
      <Form onSubmit={(value) => {}}>
        {({ register, formState }) => (
            return(
                <>
          <div>
            <div className=" w-[170px] h-[170px] bg-[#D9D9D9]"></div>
            <Camera className="text-[black]"></Camera>
          </div>


<Input></Input>
</>
            )
        )}
      </Form>
  );
};

export default ProfilePost;
